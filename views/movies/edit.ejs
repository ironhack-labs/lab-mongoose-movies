<!DOCTYPE html>
<html>
  <head>
    <title> Movies </title>
    <link rel='stylesheet' href='/stylesheets/style.min.css' />
  </head>
  <body>
    <h1> Update Your Movie </h1>

    <div class="container">

      <form class="user-input" action="/movies/<%= movie._id %>" method="post">

        <label for="title">Title</label>
        <input type="text" id="title" name="title" value="<%= movie.title %>">
          <br>
        <label for="genre">Genre</label>
        <input type="text" name="genre" id="genre" value="<%= movie.genre %>">
          <br>
        <label for="plot">Plot</label>
        <input type="text" name="plot" id="plot" value="<%= movie.plot %>">
          <br>
        <button class="create-btn" type="submit">Update Movie</button>
      </form>
    </div>

  </body>
</html>
