<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Edit a Movie</title>
  </head>
  <body>
    <h1> Edit a Movie </h1>
    <h2>Movie Details</h2>

    <ul>
      <li><h2> <%= movie.title %> </h2></li>
      <li><p><%= movie.genre %> </p></li>
      <li><p><%= movie.plot %></p></li>
    </ul>
    <a href="/movies/"> Got Back </a>

    <form action="/movies/<%= movie._id %>" method="POST">
      <label for="title">Name</label>
      <input type="text" id="name" name="title" value="<%= movie.title %>">

      <label for="genre">Genre</label>
      <input type="text" name="genre" id="genre" value="<%= movie.genre %>">

      <label for="plot">Plot</label>
      <textarea type="text" name="plot" id="plot"><%= movie.plot %></textarea>

      <button type="submit">Update Movie</button>
    </form>

  </body>
</html>
