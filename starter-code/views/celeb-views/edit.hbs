<div class='page'>

<h2>Edit {{theActualCeleb.name}} </h2>

<form action="/celebs/update/{{theActualCeleb._id}}" method="POST">
{{!-- we are passing the ID in the url because whichever route we submit this form to --}}
{{!-- need to know which book to edit, so it needs to know the ID --}}
{{!-- the id can be passed through params like I did right here above --}}
{{!-- ir you can pass the ID through req.body (secretly) by adding a hidden input --}}



<p>Name</p>
<input type="text" name="name" value="{{theActualCeleb.name}}">

<p>occupation</p>
<input type="text" name="occupation" value="{{theActualCeleb.occupation}}" >

<p>CatchPhrase</p>
<input type="text" name="catchPhrase" value="{{theActualCeleb.occupation}}" >

<p>Image URL</p>
<input type="text" name="image" value="{{theActualCeleb.image}}">


<input type="hidden" value="{{theActualCeleb._id}}" name="theID">



    <button>Save!</button>
</form>
</div>