<h1>Edit artist info:</h1>

<form action="/movies/{{movie._id}}" method="POST">
  <label for="title">Title of the movie</label>
  <input type="text" name="title" id="" value="{{movie.title}}" placeholder="{{movie.title}}">
    <br>
  <label for="genre">Genre</label>
  <input type="text" name="genre" id="" value="{{movie.genre}}" placeholder="{{movie.genre}}">
    <br>
  <label for="plot">Plot</label>
  <input type="text" name="plot" id="" value="{{movie.plot}}" placeholder="{{movie.plot}}">
    <br>
  <button type="submit" name="button">Edit info</button>
</form>

<a href="/movies">Back to the main page -></a>