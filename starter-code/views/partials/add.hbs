<h2>Add {{title}}</h2>
<form class="form-group" action="{{path}}" method="POST">
  <label for="{{nameTitle}}">{{nameTitle}}:</label>
  <input type="text" name="{{name}}">
  <label for="{{typeTitle}}">{{typeTitle}}:</label>
  <input type="text" name="{{type}}">
  <label for="{{aboutTitle}}">{{aboutTitle}}:</label>
  <input type="text" name="{{about}}">
  <button type="submit">ADD</button>
</form>
<a class="btn btn-primary" href="{{path}}" role="button">BACK to {{tipology}}</a>